// The near-monochrome palette from SPEC.md §9. Color appears in exactly
// three places: subject ticks (subjectColors.ts), each student's own
// accentColor, and this single warm amber for roll marks / "Show Mom".
export const COLORS = {
  background: "#FAF7F2",
  text: "#1A1A1A",
  muted: "#6B6B6B",
  mutedFaint: "#B8AF9F",
  hairline: "#DDD6CB",
  amber: "#B5451B",
} as const;
